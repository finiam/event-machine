{
  "name": "event-machine",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ finiam/event-machine",
    "create-local": "graph create --node http://localhost:8020/ finiam/event-machine",
    "remove-local": "graph remove --node http://localhost:8020/ finiam/event-machine",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 finiam/event-machine"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.22.4",
    "@graphprotocol/graph-ts": "^0.22.1"
  }
}
